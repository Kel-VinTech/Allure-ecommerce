import { MainContent, HeroContent } from "../../components";

const Home = ( ) => {
    return (
        <main>
            <HeroContent />
            <MainContent />
        </main>
    )
}

export default Home;