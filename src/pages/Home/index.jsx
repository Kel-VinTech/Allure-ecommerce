import { MainContent, HeroContent, BlogContent } from "../../components";

const Home = ( ) => {
    return (
        <main>
            <HeroContent />
            <MainContent />
            <BlogContent />
        </main>
    )
}

export default Home;