import { MainContent, HeroContent, BlogContent,LearnAbout } from "../../components";

const Home = ( ) => {
    return (
        <main>
            <HeroContent />
            <MainContent />
            <LearnAbout />
            <BlogContent />
        </main>
    )
}

export default Home;